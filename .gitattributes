# 行尾统一成 LF。
#
# 不只是洁癖：Dockerfile 里的 `RUN ... \` 续行在 CRLF 下会失效，
# 而本机 git 的 core.autocrlf 默认会把 checkout 出来的文件转成 CRLF——
# 于是「谁在 Windows 上 clone 的」会决定容器能不能构建出来。
* text=auto eol=lf

# 二进制不做行尾转换，否则文件会被改坏。
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary
*.zip binary
*.woff binary
*.woff2 binary
