# 依赖与产物都在镜像内重新生成，绝不要进入构建上下文。
node_modules
**/node_modules
apps/web/dist
data
.git
.gitignore
*.log
.env
.env.local
e2e-*.mjs
# 绿色包 / 安装程序的产物（自带一个 Node 与几十 MB 的 zip）与镜像无关：
# 它们在**外面**发，进构建上下文只会把镜像撑大。
dist-desktop
# 验收用例与一次性工具：它们只在开发机上跑，镜像里用不着（服务端只用 Node 内置模块，
# 跑测试也不需要镜像里多任何东西）。
tests
tools
data-cloud
